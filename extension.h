#ifndef EXTENSION_CHECKER_H
#define EXTENSION_CHECKER_H

// Header file for declaring the function that checks extensions and returns the corresponding directory name.
//  Function declaration
const char *get_group_directory(const char *extension);

#endif